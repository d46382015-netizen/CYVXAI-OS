name: Apply Universal Operator Source Fix

on:
  pull_request:
    paths:
      - "scripts/apply-universal-operator-fix.js"
      - ".github/workflows/universal-operator-source-fix.yml"

permissions:
  contents: write

concurrency:
  group: universal-operator-source-fix-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out pull request branch
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Apply deterministic source correction
        run: node scripts/apply-universal-operator-fix.js

      - name: Commit corrected universal operator
        shell: bash
        run: |
          set -Eeuo pipefail
          if git diff --quiet -- services/operator/universal.js; then
            echo "Source already corrected"
            exit 0
          fi
          git config user.name "CYVX Production Bot"
          git config user.email "actions@users.noreply.github.com"
          git add services/operator/universal.js
          git commit -m "Complete activation before outcome evaluation"
          git push origin "HEAD:${{ github.event.pull_request.head.ref }}"