# CYVX Connectors

Connectors are safe integration adapters for external systems.

Default policy:
- no secrets in source
- env-only credentials
- allowlisted actions
- audited execution
- fallback-safe behavior
