# Spark + CYVX Public Deployment

Unified public service routes:

- `/` Spark
- `/w/:slug` operational Worlds
- `/os` CYVX OS
- `/healthz` health

Deployment automation lives in `render.yaml` and `.github/workflows/deploy-public.yml`.
