apiVersion: v1
kind: Service
metadata:
  name: cyvx-ui
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: cyvx-ui
  ports:
    - name: http
      port: {{ .Values.service.uiPort }}
      targetPort: 8080
  type: LoadBalancer
