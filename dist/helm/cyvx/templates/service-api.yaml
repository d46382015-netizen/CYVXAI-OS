apiVersion: v1
kind: Service
metadata:
  name: cyvx-api
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: cyvx-api
  ports:
    - name: http
      port: {{ .Values.service.apiPort }}
      targetPort: {{ .Values.service.apiPort }}
  type: ClusterIP
