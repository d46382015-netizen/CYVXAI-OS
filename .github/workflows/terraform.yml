name: Terraform

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  terraform:
    name: Terraform Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate placeholder Terraform file exists
        run: |
          test -f main.tf
          echo "Terraform workflow is manual-only until real infrastructure is configured."
