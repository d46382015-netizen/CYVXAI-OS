# CYVX Agency Daemon

The autonomous agency can now tick without a user prompt.

## Loop

Timer → AEON Council → Mission → Decision → Memory → Log → State

## Commands

npm run agency:tick -- "Improve my business"
npm run agency:daemon

## API

GET /api/v1/agency-daemon
POST /api/v1/agency-daemon/tick
